# Deploy network and a VM with VPN software


This setup uses WIREGUARD and STRONGSWAN configured via bash

This is a very complicated setup with lots of issues

It is parked until later, as OpenSense / pfSense or other appliances are a better move
but does require som configuration outside terraform..
